# task-003 — FE 기능 연동 · 통합 검증 (F6 · F7 FE)

> 선행 : task-001 (UI 구조) · task-002 (BE 로직) 완료
> 범위 : Phase-10 ~ Phase-13
> 상태 표기 : ✅ 완료 · ❌ 미착수 · ⚠️ 확인 필요 / 보류

---

## 상태 모델 (`state/store.ts`)

| 상태 | 내용 | 변경 시점 |
|---|---|---|
| `files` | 업로드 PNG 목록 | 업로드 · 칩 제거 · 초기화 |
| `ocrRows` | OCR 결과 | OCR 실행 · 초기화 |
| `judge` | 판정 결과 (`rows` · `summary` · `cf_db`) | 판정 실행 · 자동 재판정 · 재판독/초기화 시 `null` |
| `cfDb` | CF 입력값 (기본 −50.0) | 입력 |
| `limits` | 활성 Limit (`rows` · `source`) | 모달 로드 · 저장 · 복원 |
| `health` | Key 로드 / dry-run / 연결 실패 | 기동 시 |

| 파생 조건 | 규칙 (PRD F6) |
|---|---|
| STEP 2 잠금 해제 | `ocrRows.length ≥ 1` (규칙 1 · 3) |
| 판정 실행 활성 | `OK` 행 ≥ 1 (규칙 3) |
| STEP 3 잠금 해제 · 다운로드 활성 | `judge ≠ null` (규칙 2 · 7) |

---

## Phase-10 STEP 1 (이미지 판독)

| # | task | 상태 | 비고 |
|---|---|---|---|
| 10.1 | 드래그앤드롭 · 클릭 업로드 · PNG 필터 · 다중 선택 · 드래그 오버 강조 | ❌ | primary-container |
| 10.2 | 파일 칩 목록 · 개별 제거 | ❌ | |
| 10.3 | OCR 실행 → multipart `POST /api/ocr` · 진행 바 + "이미지 판독 중" 라벨 | ❌ | |
| 10.4 | 결과 표 : 파일명 · Center(GHz) · Mkr1 주파수(GHz) · Mkr1 전력(µW) · 측정시각 · 상태 배지 (`OK` / `OCR 실패`) | ❌ | 읽기 전용 |
| 10.5 | Key 미설정 400 → 안내 토스트 | ❌ | Q1 |
| 10.6 | 재판독 → `judge = null` · STEP 3 재잠금 | ❌ | 규칙 4 |
| 10.7 | 초기화 → 파일 · OCR · 판정 전체 리셋 · STEP 2 · 3 재잠금 | ❌ | 규칙 6 |

## Phase-11 STEP 2 (판정) · Limit 모달 (F7)

| # | task | 상태 | 비고 |
|---|---|---|---|
| 11.1 | 잠금 해제 · 판정 실행 활성 조건 적용 | ❌ | 상태 모델 참조 |
| 11.2 | CF 입력 (숫자 검증 · 기본 −50.0) | ❌ | 오류 시 input-error |
| 11.3 | 판정 실행 → `POST /api/judge` (OCR 결과 재사용) | ❌ | |
| 11.4 | KPI 4타일 : 총 건수 · PASS · FAIL · 종합판정 · 좌측 4px 컬러 바 + 아이콘 | ❌ | |
| 11.5 | 결과 표 9컬럼 : 순번 · 파일명 · 주파수 · 측정전력 · dBm · dBµV/m · Limit · 마진 · 판정 배지 | ❌ | FAIL 행 좌측 4px 바 |
| 11.6 | CF 변경 → 판정 존재 시에만 자동 재판정 · `/api/ocr` 재호출 금지 | ❌ | 규칙 5 · change 이벤트 기준 |
| 11.7 | Limit 모달 열기 → `GET /api/limits` · 출처 배지 (`기본값` / `사용자 수정`) | ❌ | |
| 11.8 | 인라인 수정 · 행 추가 · 행 삭제 | ❌ | |
| 11.9 | FE 선검증 : 하한 < 상한 · 0 초과 · 구간 중복 · limit 숫자 · 0행 → 오류 셀 표시 · 저장 차단 | ❌ | BE 규칙 동일 |
| 11.10 | 저장 → `PUT /api/limits` · 성공 토스트 · 판정 존재 시 자동 재판정 | ❌ | 규칙 5 |
| 11.11 | 400 응답 → 위반 행 · 사유 토스트 · 기존 값 유지 | ❌ | |
| 11.12 | 기본값 복원 → `POST /api/limits/reset` · 표 갱신 | ❌ | |
| 11.13 | 복원 후 자동 재판정 여부 | ⚠️ | PRD 미정의 · 저장과 동일 처리 잠정 |

## Phase-12 STEP 3 (보고서 작성)

| # | task | 상태 | 비고 |
|---|---|---|---|
| 12.1 | 잠금 해제 · 다운로드 활성 = 판정 결과 존재 | ❌ | 규칙 2 · 7 |
| 12.2 | 메타 입력 : 담당자 (`tester`) · 시료명 (`sample_name`) · 검토자 (`reviewer`) | ❌ | 필수 여부 미정의 ⚠️ · 빈 값 허용 잠정 |
| 12.3 | Excel 다운로드 → `POST /api/export/xlsx` → Blob 저장 `re_result.xlsx` | ❌ | |
| 12.4 | DOCX 다운로드 → `POST /api/export/docx` → Blob 저장 `re_report.docx` | ❌ | |
| 12.5 | 완료 토스트 (pass 아이콘 · 4초 소멸) · 실패 토스트 | ❌ | |

## Phase-13 통합 검증 · 마무리

| # | task | 상태 | 비고 |
|---|---|---|---|
| 13.1 | E2E : 업로드 → OCR 실행 → 판정 실행 → 메타 입력 → 다운로드 2종 | ❌ | img_1 ~ 3 |
| 13.2 | 잠금 : OCR 전 STEP 2 · 3 / 판정 전 STEP 3 | ❌ | |
| 13.3 | 재판독 시 이전 판정 초기화 | ❌ | |
| 13.4 | CF · Limit 변경 시 `/api/ocr` 재호출 0회 (네트워크 탭) | ❌ | |
| 13.5 | Limit 편집 → 저장 → 새 기준 마진 · 판정 반영 / 잘못된 입력 → 토스트 · 기존 값 유지 / 복원 → §3.1 | ❌ | |
| 13.6 | 판정 표기 = 색 + 텍스트 라벨 + 아이콘 | ❌ | DESIGN 판정색 규칙 |
| 13.7 | BE 중지 상태 API 호출 → 토스트 · 화면 멈춤 없음 · 헤더 연결 실패 | ❌ | |
| 13.8 | `npm run build` 에러 0 · `typecheck` · `lint` 오류 0 | ❌ | |
| 13.9 | 배치 스크립트 : `run_frontend.bat` · `run_all.bat` (CRLF) | ❌ | `run_backend.bat` ✅ 기존 |
| 13.10 | 테스트 종료 후 5173 · 8000 프로세스 종료 | ❌ | |

---

## PRD 통과 조건 추적

| PRD | 통과 조건 | task |
|---|---|---|
| F1 | health 200 · Key 미설정 `api_key_loaded: false` | 1.6 ✅ |
| F1 | Key 미설정 `/api/ocr` → 400 + 안내 | 7.1 · 10.5 |
| F1 | FE 오리진 CORS 허용 | 1.6 ✅ · 5.1 |
| F1 · F7 | limits 기본 4행 · PUT 후 재기동 유지 · reset 복귀 | 6.10 |
| F2 | 3장 `OK` · 화면 값 일치 | 7.9 |
| F4 | CF −50 → FAIL 1 · PASS 2 · 종합 FAIL / CF −52 → 전부 PASS | 8.10 |
| F5 | docx 1페이지 · `{{` 0건 · 한글 깨짐 0건 · 행 수 일치 | 9.9 |
| F6 | E2E · 잠금 · 재판독 초기화 · OCR 재호출 0회 | 13.1 ~ 13.4 |
| F6 | 빌드 · 타입 · 린트 0건 · 판정 색 + 라벨 · 실패 토스트 | 13.6 ~ 13.8 |
| F7 | 편집 → 재판정 반영 · 잘못된 입력 거부 · 기본값 복원 | 13.5 |
